# Windows 桌面管理器设计文档

  ## 1. 产品定位

  以桌面容器为核心的 Windows 桌面工作区管理器。

  产品目标：

  - 整理桌面图标和文件
  - 降低桌面视觉复杂度
  - 保持操作流畅、低资源占用
  - 支持后续增加搜索、规则、同步和插件能力

  ## 2. 设计原则

  1. 性能优先：后台低占用，交互无明显卡顿。
  2. 数据安全：普通容器不移动真实文件；涉及真实文件的操作必须明确反馈。
  3. 渐进增强：首版保持核心简单，后续能力通过模块扩展。
  4. 状态可恢复：布局、配置和操作支持保存、恢复和撤销。
  5. 视觉清晰：容器边界明确，信息密度适中，支持明暗主题和高对比度模式。
  6. Windows 融合：兼容原生桌面图标、资源管理器和多显示器环境。

  ## 3. 首版功能范围

  ### 3.1 普通容器

  普通容器用于管理图标分组：

  - 创建、删除、重命名
  - 移动和缩放
  - 拖入、拖出和批量放入
  - 图标网格排列
  - 图标大小调整
  - 折叠、隐藏和锁定
  - 自定义颜色和透明度

  普通容器只保存图标分组关系，不移动真实文件。

  ### 3.2 文件夹容器

  文件夹容器映射一个本地 Windows 文件夹：

  - 实时显示文件夹内容
  - 支持打开原文件夹
  - 支持文件拖入和拖出
  - 真实文件移动、删除前给出明确提示
  - 删除容器不会删除对应文件夹和内容

  首版只支持一个容器绑定一个本地文件夹。

  ### 3.3 桌面与布局

  - 默认保留 Windows 原生桌面图标
  - 支持多显示器
  - 保存和恢复容器位置、大小及显示器归属
  - 支持分辨率变化后的布局恢复
  - 支持双击桌面隐藏全部容器
  - 支持快捷键隐藏和恢复容器

  ### 3.4 首次启动

  默认创建：

  常用应用
  工作
  文件
  临时区域

  不自动移动用户现有图标。用户可以选择：

  - 保持现状
  - 放入“未分类”容器
  - 根据类型生成整理建议

  ## 4. 文件与操作规则

   操作        普通容器        文件夹容器
  ━━━━━━━━━━  ━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━
   拖入文件    建立图标归属    移动真实文件
  ──────────  ──────────────  ──────────────────
   拖出文件    移除容器归属    移动真实文件
  ──────────  ──────────────  ──────────────────
   删除图标    删除分组关系    删除文件前确认
  ──────────  ──────────────  ──────────────────
   删除容器    文件保留        文件夹和内容保留
  ──────────  ──────────────  ──────────────────
   打开项目    打开原路径      打开原路径

  涉及真实文件的操作应支持撤销，文件变化应提供清晰的状态反馈。

  ## 5. 性能目标

  首版以中等配置 Windows 设备为基准：

  - 后台空闲 CPU 平均占用目标：低于 1%
  - 后台内存占用目标：低于 100 MB
  - 应用启动时间目标：1.5 秒内
  - 容器拖拽和窗口调整保持稳定帧率
  - 普通交互反馈目标：100 ms 内
  - 文件夹变化采用增量更新，不重复扫描全部目录
  - 文件监控、布局保存和缩略图处理不得阻塞 UI 线程
  - 大量图标时采用虚拟化、延迟加载和批量更新

  性能指标需要通过真实数据持续验证，不能只依赖主观体验。

  ## 6. 技术架构要求

  建议采用分层、事件驱动架构：

  UI 层
    ├── 桌面容器视图
    ├── 设置界面
    └── 首次启动引导

  应用层
    ├── 容器管理
    ├── 拖拽与命令
    ├── 布局管理
    └── 撤销与恢复

  领域层
    ├── 普通容器模型
    ├── 文件夹容器模型
    ├── 图标模型
    └── 显示器布局模型

  平台层
    ├── Windows 桌面图标
    ├── 文件系统监控
    ├── 快捷键
    ├── 多显示器
    └── Shell 集成

  基础设施层
    ├── 配置存储
    ├── 数据迁移
    ├── 日志
    └── 性能监控

  架构必须满足：

  - UI 与业务逻辑分离
  - 平台能力通过接口封装
  - 数据格式支持版本号和迁移
  - 用户操作使用命令模型，便于撤销和扩展
  - 文件监控、搜索、规则等能力以独立服务接入
  - 后续支持插件或功能模块时不修改核心容器模型

  ## 7. UI 设计要求

  视觉方向采用 Fluent 风格与清晰卡片边界：

  - 圆角容器
  - 半透明背景
  - 清晰边框
  - 标题栏始终可识别
  - 鼠标悬停显示操作按钮
  - 支持浅色、深色和跟随系统主题
  - 支持透明度关闭
  - 支持高对比度模式
  - 动画短而克制，低性能设备可关闭
  - 容器标题、图标和操作按钮保持足够点击区域
  - 避免过度装饰，优先保证图标辨识和操作效率

  ## 8. 暂缓功能

  以下功能不进入首版：

  - 全局搜索
  - 云端同步
  - 自动规则移动文件
  - 壁纸引擎
  - 桌面小组件
  - 插件市场
  - 复杂窗口管理
  - 动态视觉特效

  架构需要为这些功能预留扩展接口。

  ## 9. 首版验收标准

  首版完成需满足：

  - 用户可以创建并管理普通容器
  - 用户可以创建文件夹容器并看到文件变化
  - 普通容器不会意外移动或删除真实文件
  - 容器布局在应用重启后保持
  - 多显示器环境下布局可正确恢复
  - 大量图标时拖拽和滚动仍保持流畅
  - 应用后台运行时资源占用稳定
  - 设置、布局和容器数据具备版本迁移能力
  - 明暗主题、高对比度和透明度设置可用
